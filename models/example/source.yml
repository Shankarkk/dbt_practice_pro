version: 2
sources:
   - name: datafeed_shared_schema
     database: "{{('practice_dbt')}}"
     schema: raw_table

     tables:
      - name: RAW_CUSTOMERS
      - name: RAW_PAYMENTS
      - name: RAW_ORDERS
